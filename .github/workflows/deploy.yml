name: Runner Logs Demo

on:
  push:
    branches:
      - main

jobs:
  log-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print message
        run: echo "Hello from GitHub Actions Runner"

      - name: Show current directory
        run: pwd

      - name: List files
        run: ls -la
